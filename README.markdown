Albemuth's Gosu Vim Config
==========================


Installation:

git clone git://github.com/albemuth/gosu_vim_config.git
cd gosu_vim_config

ln -s vim ~/.vim
ln -s vimrc ~/.vimrc

git submodule init
git submodule update

